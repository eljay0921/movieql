type Person {
  id: Int!
  name: String!
  age: Int!
  gender: String!
}

type Query {
  # Person Array (Required)
  people: [Person]!

  # Just One Person by Person's ID (Required) => Person (Optional) 해당하는 Person이 없을 수도 있으니까
  person(id: Int!): Person
}
